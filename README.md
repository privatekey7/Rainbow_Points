# Rainbow Points

Получает поинты Rainbow Wallet на кошельки с не ненулевым балансом

## 🚀 Возможности

- ✅ Автоматический импорт кошельков через приватные ключи
- ✅ Проверка баланса поинтов для каждого кошелька
- ✅ Безопасное шифрование приватных ключей
- ✅ Поддержка прокси для работы с большим количеством кошельков
- ✅ База данных SQLite для хранения результатов
- ✅ Статистика и экспорт данных в Excel
- ✅ Пропуск уже обработанных кошельков
- ✅ Удобное консольное меню

## 📋 Требования

- Node.js >= 18.0.0
- npm или yarn
- Расширение Rainbow Wallet (папка `Rainbow-Chrome`)

## 🔧 Установка

1. Клонируйте репозиторий или скачайте проект и распакуйте расширение Rainbow-Chrome.zip в корень проекта

2. Установите зависимости:
```bash
npm install
```

3. Распакуйте расширение Rainbow-Chrome.zip в корень и убедитесь, что папка `Rainbow-Chrome` с расширением Rainbow Wallet находится в корне проекта

## ⚙️ Настройка

### Приватные ключи

Заполните файл `keys.txt` в корне проекта и добавьте приватные ключи (по одному на строку):

```
0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890
```

**Важно:** Для безопасности рекомендуется зашифровать ключи. При первом запуске программа предложит зашифровать файл `keys.txt`.

### Прокси

Заполните файл `proxy.txt` в корне проекта и добавьте прокси в формате:
```
host:port:username:password
host2:port2:username2:password2
```

Пример:
```
proxy.example.com:8080:user:pass
192.168.1.1:3128:admin:secret
```

Если файл не найден, программа будет работать без прокси.

## 🎯 Использование

### Запуск программы

```bash
npm start
```

Или для разработки:
```bash
npm run dev
```

### Меню программы

После запуска откроется консольное меню с опциями:

1. **Запустить** — начинает обработку всех кошельков из `keys.txt`
2. **Статистика** — показывает статистику по обработанным кошелькам
3. **Выход** — завершает работу программы

### Процесс обработки

1. Программа загружает приватные ключи из `keys.txt` или зашифрованных файлов
2. Перемешивает ключи в случайном порядке
3. Для каждого ключа:
   - Запускает браузер с расширением Rainbow Wallet
   - Импортирует кошелек через приватный ключ
   - Устанавливает пароль для кошелька
   - Проверяет баланс поинтов
   - Сохраняет результат в базу данных
4. Пропускает уже успешно обработанные кошельки (с поинтами)
5. Выводит итоговую статистику

### Просмотр статистики

В меню выберите опцию **Статистика** для просмотра:
- Общего количества кошельков
- Количества кошельков с поинтами
- Количества кошельков без поинтов
- Количества ошибок
- Общей суммы поинтов
- Среднего количества поинтов на кошелек
- Детальной таблицы всех кошельков

Также можно экспортировать данные в Excel файл.

## 🔐 Безопасность

### Шифрование ключей

При первом запуске программа предложит зашифровать файл `keys.txt`. После шифрования:

- Создаются файлы `keys.encrypted` и `keys.salt`
- При каждом запуске потребуется ввод пароля для расшифровки
- Рекомендуется удалить файл `keys.txt` после шифрования

**Алгоритм шифрования:** AES-256-CBC с PBKDF2 (100,000 итераций)

### Хранение данных

- Приватные ключи хранятся только локально
- База данных SQLite хранится в файле `points.db`
- Все чувствительные файлы добавлены в `.gitignore`

## 📊 База данных

Программа использует SQLite базу данных (`points.db`) для хранения:

- Адреса кошельков
- Количество поинтов
- Статус обработки (success, error, empty_balance)
- Сообщения об ошибках
- Даты проверки

### Статусы кошельков

- `success` — кошелек успешно обработан, поинты найдены
- `empty_balance` — кошелек обработан, но баланс пуст
- `error` — произошла ошибка при обработке
- `pending` — кошелек еще не обработан

## 📁 Структура проекта

```
RainbowPoints/
├── src/
│   ├── rainbow.ts          # Основной модуль автоматизации
│   ├── database.ts          # Работа с базой данных
│   ├── key-encryption.ts    # Шифрование приватных ключей
│   ├── proxy-manager.ts     # Управление прокси
│   ├── menu.ts              # Консольное меню
│   └── encoding-setup.ts    # Настройка кодировки
├── Rainbow-Chrome/          # Расширение Rainbow Wallet
├── keys.txt                 # Приватные ключи (опционально)
├── keys.encrypted           # Зашифрованные ключи
├── keys.salt                # Соль для шифрования
├── proxy.txt                # Прокси (опционально)
├── points.db                # SQLite база данных
├── package.json
├── tsconfig.json
└── README.md
```

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что папка `Rainbow-Chrome` с расширением находится в корне проекта
3. Проверьте формат файлов `keys.txt` и `proxy.txt`
4. Убедитесь, что используете Node.js версии 18 или выше

## 📈 Статистика

Программа автоматически собирает статистику:
- Общее количество обработанных кошельков
- Количество кошельков с поинтами
- Количество ошибок
- Общая сумма поинтов
- Среднее количество поинтов

Все данные можно экспортировать в Excel для дальнейшего анализа.
